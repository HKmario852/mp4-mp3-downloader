# mp4/mp3 downloader 0.2.7

Windows 與 Android 的 MP3 封面只會嵌入音訊標籤，下載目的地不再另外產生 `cover.jpg` 或以歌曲名稱命名的 JPG。影片保留縮圖的選項維持有效。手動在標籤編輯器更換封面亦只會更新 MP3 內的封面。

Windows 的標籤編輯清單依下載完成時間排序，最新歌曲顯示在最上面；手動加入的檔案按檔案修改時間排列。搜尋欄的提示文字與輸入游標對齊。切換至英文後，下載任務搜尋與貼上網址欄的提示文字會改為英文。

此更新不會自動刪除舊版本已產生的 JPG；使用者可自行檢查並移除不需要的舊檔。
